{
  "name": "@sih/rule-engine-ts",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic TypeScript evaluator over the shared rule pack (plan §5.2)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ."
  }
}
