# Dependencies
node_modules/
venv/
.venv/
__pycache__/
*.py[cod]

# Build output
dist/
build/
*.egg-info/

# Environment
.env
.env.local

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Logs
*.log

# TypeScript build info
*.tsbuildinfo

# Expo / React Native
.expo/
mobile/android/
mobile/ios/
*.keystore
*.apk
*.aab

# Test / coverage / tooling caches
coverage/
.pytest_cache/
.ruff_cache/
.vitest/
playwright-report/
test-results/

# Eval artefacts — gold images are tracked, generated reports are not
eval/reports/
eval/out/

# Typecheck output for test projects (never shipped)
.tscache/

# tesseract.js caches language data into the working directory when run under Node
/dashboard/eng.traineddata

# OCR benchmark inputs are regenerated from bench/mk.html, not committed
/dashboard/bench/*.png

# Local API keys — never committed
.env.local
.env.*.local
